services:
    free-claude-code:
        #build: .
        image: free-claude-code:latest
        container_name: free-claude-code
        ports:
            - "8086:8082"
        env_file:
            - .env
        restart: unless-stopped
